<?

require_once("../include/ScriptManager.php");

$scriptManager = new ScriptManager();
$scriptManager->usage( array(), true, true );
global $db;
$db = $scriptManager->getDatabase();


CURSOR


return;

/*******
 *
 *  
 *
 ******/

?>
